# .env (環境變量配置文件)
# 註釋以 '#' 開頭

# 認證網關 (start.cjs) 將監聽的公開端口
PUBLIC_PORT=8100

# 主應用程序 (server.js) 監聽的內部端口
# start.cjs 會將請求代理到這個端口
APP_INTERNAL_PORT=3200

# Node.js 環境模式 (可選)
# 設置為 'development' 可以幫助調試，例如在錯誤頁面顯示詳細的錯誤堆棧
# 在生產環境中，通常會設置為 'production'
NODE_ENV=development

# 您可以在此處添加其他配置，例如：
# DATABASE_URL=your_database_connection_string
# API_KEY=your_api_key
